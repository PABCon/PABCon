let add7 = function(num) {

    { return num + 7 }

}

let multiply = function(num1, num2) {

    return num1 * num2;
}

let capitalize = function(str) {

    str = str.toLowerCase();

    return str[0].toUpperCase() + str.slice(1, str.length)


}

let lastLetter = function(str) {

    console.lot(str.length);

}